<p class='text-xl'>Hello, <%= @user.name %>!</p>
<%= button_to "Logout", session_path, method: :delete, class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded' %>

<%= link_to 'Feed List', feeds_path, class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 m-1 rounded' %>



<%= render 'articles/articles', articles: @articles if @articles.present? %>
